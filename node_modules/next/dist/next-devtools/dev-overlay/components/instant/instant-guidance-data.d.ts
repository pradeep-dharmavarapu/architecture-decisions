export type CardColor = 'blue' | 'purple' | 'red' | 'amber' | 'teal' | 'gray';
export type FixCardGroup = 'stream' | 'prerender' | 'block' | 'cache' | 'static' | 'dynamic' | 'client' | 'defer' | 'measure';
export type FixCardIcon = 'align-left' | 'database' | 'history' | 'layout' | 'octagon' | 'pointer-click' | 'server-stack' | 'timer' | 'zap';
export declare const FIX_CARD_GROUPS: Record<FixCardGroup, {
    label: string;
    color: CardColor;
    icon: FixCardIcon;
}>;
export type FixCard = {
    /** Stable docs-anchor id for this fix card. */
    id: string;
    title: string;
    group: FixCardGroup;
    /** Docs URL the card links to, or `null` for no link. */
    link: string | null;
    snippets: Snippet[];
};
export type SnippetPart = {
    text: string;
    highlight?: boolean;
};
export type Snippet = {
    text: string;
    highlight?: boolean;
    parts?: SnippetPart[];
};
export type GuidanceKind = 'blocking-route' | 'metadata' | 'viewport' | 'sync-io' | 'sync-io-client';
export type GuidanceVariant = 'runtime' | 'navigation';
export declare const DOCS_URLS: Record<GuidanceKind, string>;
export declare const SYNC_IO_DOCS: Record<string, string>;
export declare const SYNC_IO_CLIENT_DOCS: Record<string, string>;
export declare const EXPLANATIONS: Record<GuidanceKind, string>;
export declare function getCards(kind: GuidanceKind, variant: GuidanceVariant, cause?: string): FixCard[];
